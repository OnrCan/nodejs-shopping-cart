extends ../layout

block content
  .container
    .row
      each product in products
        .col-sm-6.col-md-4
          .card(style='')
            img.card-img-top(src= product.imagePath, alt='Card image cap')
            .card-body
              h5.card-title= product.title
              p.card-text= product.description
              span.price.pull-left= `£${product.price}`
              a.btn.btn-success.pull-right(href='#') Add to Cart